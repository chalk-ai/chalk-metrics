[package]
name = "chalk-metrics-macros"
version = "0.0.0"
edition = "2024"
license = "Apache-2.0"
description = "Proc macros for chalk-metrics"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "2.0", features = ["full"] }
